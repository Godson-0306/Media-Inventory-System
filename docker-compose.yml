services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: assetops
      POSTGRES_PASSWORD: assetops
      POSTGRES_DB: assetops
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
